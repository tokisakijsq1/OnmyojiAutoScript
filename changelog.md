# Tokisaki 分支更新日志（changelog）

本分支 `fix/mumu-adb-root` 基于官方 master 构建，包含以下 6 个提交（2026-09-15 ~ 2026-09-20）。
每个功能的使用方法写在对应条目内。

---

## 2026-09-20

### 结界挂卡：斗鱼/太鼓星级优先排序 + 严格模式 (`06ec297f`, `4a4b49a8`, `8687e942`)

**功能说明**

新增"挂卡星级优先级"功能：不再按默认的"每小时收益选最大"挂卡，而是按照你指定的优先级顺序
（例如 `斗鱼4星>太鼓5星`）遍历寮结界卡列表，挂上排最前、能找到的那张卡。
配套新增"严格排序模式"开关：开启后，若整个卡列表里一张排序中的卡都没有，
**本次不挂任何卡、不回退默认方式**，推送通知提醒人工挂卡，按失败间隔自动重试。

**使用方法**

1. 打开 OAS，进入 **阴阳寮 → 结界挂卡 → 挂卡规则**（ActivationConfig）。
2. 在新增的 **「挂卡星级优先级(可选)」** 输入框填写排序表达式：
   - 用 `>` 分隔，越靠前优先级越高；
   - 只支持斗鱼/太鼓的 3~6 星，写法示例：
     - `斗鱼4星>太鼓5星` —— 先找斗鱼4星，没有再找太鼓5星
     - `太鼓6>太鼓5>斗鱼6>斗鱼5` —— 只挂高星卡
   - 可以带"星"字也可以不带（`太鼓5` 等价 `太鼓5星`），空格和换行会被忽略。
3. （可选）打开 **「严格排序模式(不回退)」** 开关：
   - 开启：列表中没有排序中的卡时，不挂卡、推送通知提醒人工处理，按失败间隔重试；
   - 关闭（默认）：找不到时自动回退为默认挂卡方式（按每小时收益选最大）。
4. 注意：填写了星级优先级后，原本的放卡规则（card_type）与最低收益阈值
   （min_taiko_num / min_fish_num）**不再触发**；留空则完全不启用本功能，行为与官方版一致。

---

## 2026-09-19

### 结界蹭卡：指定好友名称寄养，失败自动回退 (`d9fac57c`)

**功能说明**

新增"指定好友寄养"：填写好友全名后，脚本会用 OCR 在寮结界的好友列表里滑动查找该好友，
优先蹭**他**的结界卡。找不到该好友、他的寄养位被占用、或寄养失败时，
自动回退为常规蹭卡流程（按蹭卡规则选最优卡），不会白跑一次。

**使用方法**

1. 打开 OAS，进入 **阴阳寮 → 结界蹭卡 → 寄养配置**（UtilizeConfig）。
2. 在新增的 **「指定好友名称」** 输入框填写好友的**完整名字**
   （基于 OCR 识别，建议一字不差；只在上方"跨区/同服好友列表"所选的列表里查找）。
3. 保持新增的 **「启用指定好友寄养」** 开关为开启状态；
   临时不想蹭这位好友时，把它关掉即可（关掉后即使填了名字也只执行常规蹭卡）。
4. 行为细节：
   - 优先点击好友名字同一行的结界卡图标进入（与人工操作一致的手势）；
   - 进入好友结界前会先检查寄养位是否被占用（识别"已占用"标签），被占则立即回退；
   - 每次任务运行中指定好友只找一遍，找不到不会反复滑动整个列表；
   - 回退常规蹭卡时如果发现寄养其实已部分生效，会按成功处理，不会重复操作。

---

## 2026-09-15

### 大神签到：兼容新版 MuMu 6.x / Android 15 的 adb root 模式 (`6d860a92`)

**功能说明**

官方版的大神签到（网易大神APP福利自动领取）在老式模拟器上依赖 `su` 命令提权，
但新版 MuMu 6.x（Android 15）的系统里没有 `su`，root 体现为 adb 自身的 root 模式，
导致签到任务启动时因拿不到 root 而直接失败。本提交让脚本自动探测并适配两种 root 形态：

1. 先试老式 `su` 提权（旧版模拟器、其他模拟器不受影响）；
2. 没有_su_ 时自动执行 `adb root` 切换 adbd root 模式（新版 MuMu 6.x / Android 15）；
3. 所有需要 root 的操作（启动 frida-server、读 auth 数据库、后台拉起大神APP等）
   统一走新的 `_root_shell`，按探测到的模式决定是否加 `su` 前缀。

**使用方法**

- 无需任何配置，行为完全自动。前提条件不变：
  1. 模拟器开启 Root 权限（新版 MuMu 在 设置中心 → 其他 里打开 Root 权限）；
  2. 模拟器中安装并登录「网易大神」APP，且绑定阴阳师角色；
- 之后正常使用 **日常任务 → 大神签到** 即可。新版 MuMu 用户如果之前一直报
  "无法获取root权限"，更新本分支后应能正常运行。
- 日志中会显示探测到的 root 模式：`Root模式: su`（老式提权）或 `Root模式: direct`（adb root）。

---

### 全局：拟人化点击延迟 (`05fdd158`)

**功能说明**

给所有任务的基础点击动作（appear_then_click / click / wait_until_appear_then_click /
appear_click 等）统一加了一个**对数正态分布的随机延迟**：中位数约 0.2 秒，
绝大多数落在 0.05~1.2 秒之间，偶尔停顿更久（模拟真人"手快有手慢无"的节奏）。

官方原来的点击间隔是固定/均匀随机的，节奏机械；本改动让两次点击的间隔呈现
真人操作特有的"大多很快、偶尔慢半拍"的右偏分布，用于降低点击行为被风控识别的概率。

**使用方法**

- 全局自动生效，无需配置、没有开关。所有任务（签到、挂卡、蹭卡、战斗等）的每次点击
  前都会插入该随机延迟。
- 对实际速度的影响很小（中位数 0.2 秒），但整体节奏更像真人。
- 若你希望调整强度，改 `tasks/base_task.py` 里 `_humanized_click_delay` 的参数即可：
  `median`（中位延迟）、`sigma`（抖动大小）、`floor`/`ceil`（上下限）。

---

## 更新方式

- **你本人**：官方有更新时，开代理 → 双击 `Tokisaki版本注入继续.bat` → 完成（自动推送 Fork）。
- **朋友**：双击 `Tokisaki更新（不推送fork）.bat`（免代理），或执行 `git pull --rebase`（需代理）。
